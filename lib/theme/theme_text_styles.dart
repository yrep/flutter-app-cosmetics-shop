part of './theme.dart';

const bodySmallStyleSettings = TextStyle(
        fontWeight: FontWeight.w400,
        fontSize: 12
      );

const customTitleSettings = TextStyle(
        fontWeight: FontWeight.w600,
        fontSize: 22
      );

const heroTitleStyleSettings = TextStyle(
        color: Colors.white,
        fontSize: 35.05,
        fontFamily: 'Vela Sans',
        fontWeight: FontWeight.w600,
        height: 0.03,
        letterSpacing: 0.35,
      );

const heroTextStyleSettings = TextStyle(
        color: Colors.white,
        fontSize: 16,
        fontFamily: 'Vela Sans',
        fontWeight: FontWeight.w400,
        height: 1.5,
        letterSpacing: 0.16,
      );

const heroButtonTextStyleSettings = TextStyle(
        color: Colors.white,
        fontSize: 12,
        fontFamily: 'Raleway',
        fontWeight: FontWeight.w600,
        height: 0.11,
      );

const sectionTitleTextSettings = TextStyle(
        color: Colors.black,
        fontSize: 12,
        fontFamily: 'Inter',
        fontWeight: FontWeight.w500,
        letterSpacing: 0.18,
      );

const productListTitleSettings = TextStyle(
        color: Colors.black,
        fontSize: 20,
        fontFamily: 'Raleway',
        fontWeight: FontWeight.w600,
        height: 1.80,
      );

const priceTextSettings = TextStyle(
        color: Colors.black,
        fontSize: 16,
        fontFamily: 'Montserrat',
        fontWeight: FontWeight.w600,
        height: 1.16,
        letterSpacing: 0.16,
      );

const crossedOutPriceTextSettings = TextStyle(
        color: Colors.black,
        fontSize: 16,
        fontFamily: 'Montserrat',
        fontWeight: FontWeight.w600,
        height: 1.16,
        letterSpacing: 0.16,
        decoration: TextDecoration.lineThrough,
        decorationThickness: 2.0,
        decorationColor: Colors.black,
      );